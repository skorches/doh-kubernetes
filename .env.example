# DNS over HTTPS (DoH) Smart DNS — Kubernetes Configuration
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# Your VPS/Node public IP address (REQUIRED)
# This is the IP that game consoles and clients will connect to
VPS_IP=

# Your domain name for the DoH endpoint (REQUIRED for HTTPS/DoH)
# Example: bypass.example.com
DOMAIN=
